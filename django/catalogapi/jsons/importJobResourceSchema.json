{
    "title": "importJobResourceSchema",
    "type": "object",
    "properties": {
	"path": { "type": "string" },
	"content-type": { "type": "string" },
	"status": { "type": "string", "enum": ["notstarted", "running", "succeeded", "failed", "completed"] },
	"url": { "type": "string", "format": "uri" },
	"completionDate": { "type": "string", "format": "date-time" },
	"creationDate": { "type": "string", "format": "date-time" },
	"errorLog": { "type": "string" }
    }
    "required": ["path", "content-type", "status", "url", "creationDate", "errorLog"]
}
